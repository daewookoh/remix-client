import { Request } from "./request.js";
export declare namespace Emails {
    type Type = Array<{
        email: string;
        verified: boolean;
        primary: boolean;
        visibility: string | null;
    }>;
    class Response {
        body: object;
        constructor(body: object);
        emails(): {
            value: string;
            type: string;
        }[];
    }
    function send(endpoint: URL, context: Request.Context, options?: {
        signal?: AbortSignal;
    }): Promise<{
        value: string;
        type: string;
    }[]>;
}
